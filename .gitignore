# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.python-version

# uv
.uv-cache/

# Env
.env
.env.local

# Generated by the assignment
data/bird/
load_test/perf_pool.jsonl

# Result JSONs: ignore dev/scratch runs, but TRACK the required deliverables
# (the two evals and the Phase 6 load tests) so they appear in the submitted repo.
results/*.json
!results/eval_baseline.json
!results/eval_after_tuning.json
!results/load_test_*.json

# screenshots/*.png intentionally NOT ignored: the required screenshots are deliverables
# that must be committed so they show up in the submitted repo.

# HF cache (if anyone runs locally)
.cache/

# IDE / OS
.DS_Store
.idea/
.vscode/

# Lecture material (not part of the student repo)
*.pdf
